# Plain Speak — Python dependencies
# Tested with Python 3.10+ on macOS, Linux, Windows.

gradio==4.44.1            # UI framework — single-file local web app
gradio_client==1.3.0      # pinned: newer gradio_client crashes on bool JSON schemas
huggingface_hub>=0.24,<1.0  # pinned <1.0: newer versions remove HfFolder
pydantic<2.10             # pinned: ensures schema compat with above Gradio/gradio_client
ollama>=0.4.4             # Python client for the local Ollama daemon
pypdf>=4.3.0              # Text extraction from PDFs (no OCR needed for text PDFs)
Pillow>=10.4.0            # Image handling — needed by both vision and OCR paths
pytesseract>=0.3.13       # OCR fallback when using text-only Gemma 4 variants
                     # Requires the Tesseract binary installed on your OS:
                     #   macOS:  brew install tesseract
                     #   Ubuntu: sudo apt install tesseract-ocr
                     #   Windows: https://github.com/UB-Mannheim/tesseract/wiki
